<section class={{profile__wrapper}}>
    <form name={{formName}}>
        <div class={{profile__top}}>
            {{{avatar}}}
            <p class={{avatar__name}}>{{profileName}}</p>
        </div>
        <div class={{profile__fields}}>
            <ul>
                {{{email}}}
                {{{login}}}
                {{{name}}}
                {{{lastName}}}
                {{{chatName}}}
                {{{phone}}}
            </ul>
            <div class={{profile__change}}>
                <ul>
                    {{{changeData}}}
                    {{{changePassword}}}
                    {{{exit}}}
                </ul>
            </div>
    </form>
    <div class={{back}}>
        {{{button}}}
    </div>
</section>
