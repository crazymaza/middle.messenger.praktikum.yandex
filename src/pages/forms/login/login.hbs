<div class={{form__wrapper}}>
    <form action="" class={{form}}>
        <p class={{form__title}}>{{title}}</p>
        <div class="{{form__inputs}} formInputs">
                {{{inputLogin}}}
                {{{inputPassword}}}
        </div>
        {{{button}}}
        <a href="/registry" class={{form__same_btn}}>{{registerLink}}</a>
    </form>
</div>
